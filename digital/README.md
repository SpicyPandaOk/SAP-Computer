Download and install Digital from the Github repo, download the .dig file from this github. Then load the DIG file into your digital on version 0.31. Make sure every ROM is connected to the correct hex file. to load a program, type it into the 
text file that the compiler uses and run the compiler. 
