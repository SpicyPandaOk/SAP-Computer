used to translate a slightly more human readable custom assembly code into hex values for the boot ROM. Input file can be any text file as long as it is in the same folder. If you don't want to change the code then name it code.txt. output file is code.mem but that can be changed in the code if need be. 
All instructions must be one per line, if the instruction has no argument just leave it blank
